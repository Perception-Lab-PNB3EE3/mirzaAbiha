?pnorm
install.packages("papaja")
setwd("C:/Users/nmirz/Downloads")
setwd("C:/Users/nmirz/Downloads/PNB 3EE3/mirzaAbiha/data visualization assignment")
data <- read.csv("assignment_dataVis_dataset.csv")
data <- read.csv("assignment_dataVis_dataset.csv")
head(data)
summary(data)
print(me)
head(data)
print(summary(data))
print(head(data))
data <- read.csv("assignment_dataVis_dataset.csv")
print(head(data))
print(summary(data))
#summary stats for x:
print(mean(data$x))
print(sd(data$x))
print(median(data$x))
#summary stats for y:
print(mean(data$y))
print(sd(data$y))
print(median(data$y))
print(mean(data$condition_1))
#first we have to load the data in, the head function lets us see the general format of the table and the summary function gives summary stats for all the data
data <- read.csv("assignment_dataVis_dataset.csv")
print(head(data))
print(summary(data))
#in order to separate the conditions in to their own columns we need to change it to a wide format
library(tidyr)
wide_data <- data %>%
pivot_wider(names_from = condition, values_from = c(x, y))
View(wide_data)
print(mean(x_condition_1))
print(mean(wide_data$x_condition_1))
wide data?
wide?
?
widedata?
#in order to separate the conditions in to their own columns we need to change it to a wide format
wide_data <- spread(data)
